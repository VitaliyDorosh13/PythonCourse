# use 3 parameters for define function from left side, and insert or use values from right side
l = (lambda x1, x2, x3 : (x1+x2+x3)/3)(9, 6, 6)

print(l)

lam = (lambda a1, a2, a3 : (a1*a2)/a3)(4, 5, 2)
print(lam)